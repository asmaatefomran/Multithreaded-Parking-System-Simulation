import java.util.ArrayList;
import java.util.List;

public class LoggingandReporting {


    // Log a status for a car (arrival, parking, waiting)
    //"synchronized" to make the function atomic and only can acessed by one thread at a time(one car)
    public synchronized void logAction(String s) {
       
    }

    // Log that car enter throw a specific gate 
    //"synchronized" to make the function atomic and only can acessed by one thread at a time(one car)
    public synchronized void logEntry(String carName, String gateName) {
        
    }

}
